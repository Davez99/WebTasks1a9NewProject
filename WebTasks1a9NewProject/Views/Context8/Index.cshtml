@model IEnumerable<WebTasks1a9NewProject.Models.Exec8>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReceberNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReceberEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReceberRG)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResponderAoUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MostrarAoUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReceberNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReceberEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReceberRG)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponderAoUser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MostrarAoUser)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
